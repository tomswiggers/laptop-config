---
- hosts: all
  remote_user: tomswiggers
  become_method: sudo
  tasks:
    - name: Ping the node
      ping:
    - name: Install mutt
      pacman:
        name: mutt, msmtp, offlineimap
        state: latest

